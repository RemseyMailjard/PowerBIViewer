<!-- FILE: Views/SettingsWindow.xaml -->
<Window x:Class="PowerBIViewer.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:PowerBIViewer.App.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel, IsDesignTimeCreatable=False}"
        Title="Instellingen" Height="600" Width="800"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource AppForegroundBrush}"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl Margin="10">
            <!-- RAPPORTEN TAB -->
            <TabItem Header="Rapporten">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Lijst met rapporten -->
                    <StackPanel Grid.Column="0">
                        <ListBox ItemsSource="{Binding Reports}" SelectedItem="{Binding SelectedReport, Mode=TwoWay}" Height="400">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock><Run Text="{Binding Emoji}"/><Run Text=" "/><Run Text="{Binding Name}"/></TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                            <Button Content="➕ Toevoegen" Command="{Binding AddReportCommand}" Margin="5" />
                            <Button Content="➖ Verwijderen" Command="{Binding RemoveReportCommand}" Margin="5" />
                        </StackPanel>
                    </StackPanel>

                    <!-- Details van geselecteerd rapport -->
                    <StackPanel Grid.Column="1" Margin="15,0,0,0" DataContext="{Binding SelectedReport}">
                        <TextBlock Text="Naam:" Margin="0,0,0,2"/>
                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                        <TextBlock Text="Key:" Margin="0,0,0,2"/>
                        <TextBox Text="{Binding Key, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                        <TextBlock Text="Emoji:" Margin="0,0,0,2"/>
                        <TextBox Text="{Binding Emoji, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                        <TextBlock Text="URL:" Margin="0,0,0,2"/>
                        <TextBox Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- WIDGETS TAB (vergelijkbaar) -->
            <TabItem Header="Widgets">
                <!-- Implementatie vergelijkbaar met Rapporten Tab, maar gebonden aan Widgets en SelectedWidget -->
                <TextBlock Text="Widget beheer komt hier." VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </TabItem>
        </TabControl>

        <!-- Onderste knoppenbalk -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Opslaan en Sluiten" Command="{Binding SaveCommand}" Style="{DynamicResource PrimaryButton}" Width="150" Margin="5"/>
            <Button Content="Annuleren" IsCancel="True" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>